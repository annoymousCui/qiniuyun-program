server:
  port: 8082

spring:
  application:
    name: ai-character-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  data:
    mongodb:
      uri: mongodb://localhost:27017/ai_roleplay
      database: ai_roleplay

management:
  endpoints:
    web:
      exposure:
        include: "*"
